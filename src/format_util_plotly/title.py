def centre_title(fig):
    fig.update_layout(
        title_x=0.5
    )


def align_left_title(fig):
    fig.update_layout(
        title_x=0.0
    )


def align_right_title(fig):
    fig.update_layout(
        title_x=1.0
    )
